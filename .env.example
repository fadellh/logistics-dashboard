# Neon Postgres connection string — from neon.tech project dashboard
# (or auto-filled if using the Vercel Storage → Neon marketplace integration)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# DeepSeek API key — from platform.deepseek.com → API Keys
# Used via the OpenAI SDK with baseURL "https://api.deepseek.com" (lib/ai/client.ts)
DEEPSEEK_API_KEY="sk-..."
